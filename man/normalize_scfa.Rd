% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_scfa.R
\name{normalize_scfa}
\alias{normalize_scfa}
\title{Normalize SCFA measurements}
\usage{
normalize_scfa(
  data,
  analytes,
  basis = c("as_is", "mmol_per_L_plasma", "mg_per_kg"),
  multiplier = 1,
  transform_fn = NULL,
  fill = 0
)
}
\arguments{
\item{data}{Tibble that contains SCFA measurements along with metadata.}

\item{analytes}{Character vector with the SCFA column names that should be
normalized.}

\item{basis}{Character string that documents the normalization strategy.}

\item{multiplier}{Numeric scalar applied to every analyte (defaults to \code{1}).}

\item{transform_fn}{Optional function that receives the numeric analyte vector
and returns the normalized values. When supplied it takes precedence over
\code{multiplier}.}

\item{fill}{Value that replaces \code{NA} entries after normalization.}
}
\value{
A tibble with normalized analyte columns and the same metadata as the
input \code{data}. The returned tibble carries \code{basis} and \code{multiplier}
attributes for downstream reporting.
}
\description{
Normalize SCFA measurements
}
\examples{
\dontrun{
normalized <- normalize_scfa(merged, analytes = default_analytes, multiplier = 7)
attr(normalized, "basis")
}
}
