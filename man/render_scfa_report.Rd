% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{render_scfa_report}
\alias{render_scfa_report}
\title{Render the SCFA Quarto report}
\usage{
render_scfa_report(
  output_file = "scfa_report.html",
  output_dir = getwd(),
  project_id = "SCFA Project",
  sample_type = "Plasma",
  scfa_data = NULL,
  manifest = NULL,
  scfa_dir = NULL,
  manifest_path = NULL,
  analytes = default_scfa_analytes(),
  normalization_multiplier = 1,
  normalization_basis = "as_is",
  stats = c("lmer", "anova", "none"),
  lmer_formula = value ~ Group * Time + (1 | Subject_ID),
  aov_formula = NULL,
  x_col = "Group",
  facet_col = "Time",
  y_label = "SCFA concentration",
  export_csv = TRUE,
  ...
)
}
\arguments{
\item{output_file}{Name of the rendered report (defaults to
\code{"scfa_report.html"}).}

\item{output_dir}{Directory where the report should be written. Defaults to
the current working directory.}

\item{project_id}{Project identifier printed in the report header.}

\item{sample_type}{Text describing the matrix (for the methods section).}

\item{scfa_data}{Optional tibble that mimics the output of
\code{\link[=read_scfa_raw]{read_scfa_raw()}}. When supplied, \code{scfa_dir} is ignored.}

\item{manifest}{Optional tibble returned by \code{\link[=read_scfa_manifest]{read_scfa_manifest()}}. When
supplied, \code{manifest_path} is ignored.}

\item{scfa_dir}{Directory with chromatogram \code{.xls} files. Ignored when
\code{scfa_data} is supplied.}

\item{manifest_path}{Path to the manifest workbook. Ignored when \code{manifest}
is supplied.}

\item{analytes}{Character vector with analyte column names. Defaults to
\code{\link[=default_scfa_analytes]{default_scfa_analytes()}}.}

\item{normalization_multiplier}{Numeric multiplier (defaults to \code{1}).}

\item{normalization_basis}{Text label describing the normalization.}

\item{stats}{Statistical engine (\code{"lmer"}, \code{"anova"}, or \code{"none"}).}

\item{lmer_formula}{Formula passed to \code{\link[lmerTest:lmer]{lmerTest::lmer()}} when
\code{stats = "lmer"}.}

\item{aov_formula}{Formula passed to \code{\link[stats:aov]{stats::aov()}} when \code{stats = "anova"}.}

\item{x_col}{Column for the x-axis in the plots.}

\item{facet_col}{Optional column used for facetting plots.}

\item{y_label}{Y-axis label for the plots.}

\item{export_csv}{Logical; when \code{TRUE} writes \code{scfa_concentration_normalized.csv}
next to the rendered report.}

\item{...}{Additional arguments forwarded to
\code{quarto::quarto_render()}, e.g. \code{quiet = TRUE}.}
}
\value{
Invisibly returns the path to the rendered report.
}
\description{
Render the SCFA Quarto report
}
\examples{
\dontrun{
render_scfa_report(
  output_file = "demo1.html",
  scfa_data = demo_scfa_dataset1,
  manifest = demo_manifest_dataset1,
  analytes = c("Acetic.Acid", "Propionic.Acid", "Butyric.Acid"),
  stats = "anova",
  aov_formula = value ~ Group
)
}
}
